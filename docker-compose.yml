services:
  workshop-8:
    build: .
    container_name: llm-workshop-8-langfuse
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped

networks:
  default:
    name: llm-workshop-network
